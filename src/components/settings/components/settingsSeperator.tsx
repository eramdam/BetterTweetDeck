import styled from '@emotion/styled';

export const SettingsSeperator = styled.hr`
  width: 95%;
  margin: 20px auto;
  border: 0;
  border-bottom: 2px solid var(--settings-modal-separator);
  opacity: 0.6;
`;
